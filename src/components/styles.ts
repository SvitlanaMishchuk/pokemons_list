import { makeStyles } from '@material-ui/core/styles';

export const useClasses = makeStyles({
    header: {
        width: '100%',
        padding: '10px 0',
        background: '#0d1a4b',
    },
    logo: {
        display: 'block',
        margin: '0 auto',
        width: '20%'
    }
});